#include <raylib.h>
#include <stdio.h>

Rectangle draw_wifi_container() 
{
        Rectangle layout;

        layout.width= 300;
        layout.height= 50;
        layout.x=      20;
        layout.y=      50;

        return layout;
}
